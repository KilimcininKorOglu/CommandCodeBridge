name: commandcode-bridge

services:
  proxy:
    build: .
    container_name: commandcode-bridge-proxy
    ports:
      - "3050:3000"
    volumes:
      - ./data/config.json:/app/config.json
      - ./data/logs:/app/data/logs
    restart: unless-stopped
